var database = require("./helpers/database");
var transportApi = require("./helpers/transportApi");

var Beacon = function(uuid, data) {
	
	// Set the UUID
	this.uuid = uuid;
	
	// Try to get the information from the database
	
	// If the beacon is not in the database, find the closest stop by position
		
		
		
	this.ge
};